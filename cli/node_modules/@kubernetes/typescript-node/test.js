"use strict";
exports.__esModule = true;
var config = require("./config");
var api = require("./api");
var k8sApi = config.Config.defaultClient();
var namespace = new api.V1Namespace();
namespace.metadata = new api.V1ObjectMeta();
namespace.metadata.name = 'test';
k8sApi.createNamespace(namespace).then(function (response) {
    console.log('Created namespace');
    console.log(response);
    k8sApi.readNamespace(namespace.metadata.name).then(function (response) {
        console.log(response);
        k8sApi.deleteNamespace(namespace.metadata.name, new api.V1DeleteOptions());
    });
}, function (err) {
    console.log('Error!: ' + err);
});
